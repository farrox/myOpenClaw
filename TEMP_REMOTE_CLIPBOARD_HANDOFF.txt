# Paste this to OpenClaw or another LLM (handoff for mypc)
# Open this file on the Ubuntu desktop from: ~/Developer/myOpenClaw/TEMP_REMOTE_CLIPBOARD_HANDOFF.txt
# After you are done with it, you can delete this file from the repo or leave it — it is not secret.

Start from ~/Developer/myOpenClaw/LLM_RESUME_CHECKPOINT.md, then ubuntu_status.md. Work in this order:

1. Smoke test — Confirm openclaw gateway run (or service) plus openclaw dashboard or openclaw tui from the environment Ed actually uses (SSH and RDP/Cinnamon if he runs the gateway there). Fix PATH / nvm for that session if openclaw is missing. Mark the gateway+chat smoke test done in ubuntu_status.md when verified.

2. pass-cli — Install Proton Pass CLI per PROTON_PASS_SETUP.md / QUICK_START_PROTON.md. Run:
   openclaw approvals allowlist add --agent main "$(command -v pass-cli || echo $HOME/.local/bin/pass-cli)"
   (use the real path from which pass-cli). Update ~/clawd/TOOLS.md if needed.

3. Optional — openclaw security audit --deep, consider tools.exec.strictInlineEval=true, sudo apt install curl if we want script parity.

4. Seller agent — With photos in /home/ed/Transfer/items/<slug>/, follow ~/clawd/SELLER_AGENT_LLM_GUIDE.md and produce listing.md (+ platform text files). Do not claim listings are posted; Ed publishes. Approve any exec prompts for tools not on the allowlist.

5. Log + git — Update ubuntu_status.md changelog/checkboxes, then from ~/Developer/myOpenClaw: git commit and git push (no secrets).

Rules: LLM_ASSISTANT_SETUP_GUIDE.md section 0 — no API keys or auth-profiles in chat/git; verify paths on mypc with bash -l -c as in LLM_RESUME_CHECKPOINT.md.

--- One-liner for OpenClaw in chat (seller only) ---
Read ~/clawd/SELLER_AGENT_LLM_GUIDE.md and ~/clawd/Transfer/items/; pick one folder with photos and draft listing.md plus paste-ready Facebook/Craigslist/eBay/OfferUp text; do not claim anything is posted.
